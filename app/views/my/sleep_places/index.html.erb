<ul>
  <% @collection.each do |entity| %>
    <li>
      <%= link_to(entity.name, edit_my_sleep_place_path(entity)) %>
      <%= button_to 'destroy', my_sleep_place_path(entity), method: :delete, data: { confirm: 'Are you sure?' } %>
    </li>
  <% end %>
</ul>

<p><%= link_to('new', new_my_sleep_place_path) %></p>
