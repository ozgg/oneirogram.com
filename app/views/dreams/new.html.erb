<% content_for :title, t('.title') %>
<% content_for :breadcrumbs do %>
  <span><%= t('.nav_text') %></span>
<% end %>

<section>
  <h1><%= t('.heading') %></h1>

  <%= render partial: 'form', locals: { model: @entity, places: @places, url: dreams_path } %>
</section>
