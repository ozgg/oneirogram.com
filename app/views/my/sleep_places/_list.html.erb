<ul>
  <% collection.each do |entity| %>
    <li>
      <%= link_to(entity.name, edit_my_sleep_place_path(entity)) %>
      <%= button_to t('actions.destroy'), my_sleep_place_path(entity), method: :delete, data: { confirm: t(:are_you_sure) } %>
    </li>
  <% end %>
</ul>
